# Minimal dependencies for training/testing the U-Net segmenter locally.
# This avoids installing the full MedGemma/LLM stack when you only want to
# verify segmentation training.

torch>=2.1.0
torchvision>=0.16.0
numpy>=1.21.0
Pillow>=10.0.0
segmentation-models-pytorch>=0.3.0
albumentations>=1.3.0
nibabel>=5.2.0
tqdm>=4.66.0
